{
  "name": "rule-engine",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "git:push": "git push --all --repo=https://github.com/absazamat/rule-engine",
    "git:pull": "git pull origin",
    "git:commit": "git commit --all -m",
    "git:commit:comment": "npm run git:commit -- 'comment'",
    "git:add": "git add --all",
    "git:clone": "git clone https://github.com/absazamat/rule-engine"
  }
}